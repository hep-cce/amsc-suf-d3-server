# Task 1.0: Fix ParticleNet PT

This model `particlenet_PT` does not run on the Triton server container created by the Dockerfile in this repository. But it works on `fastml/triton-torchgeo:26.04-py3-geometric` container. See the Dockefile: https://github.com/lgray/triton-torchgeo-gat-example/blob/master/Dockerfile.build.

Is it possible to fix the error? The error message is:

```text
evaluate(): unable to get result(s): PyTorch execute failure: The following operation failed in the TorchScript interpreter.
Traceback of TorchScript, serialized code (most recent call last):
  File "code/__torch__/utils/nn/model/ParticleNet.py", line 18, in forward
    sv_features: Tensor,
    sv_mask: Tensor) -> Tensor:
    points = torch.cat([pf_points, sv_points], 2)
             ~~~~~~~~~ <--- HERE
    _0 = (self.pf_conv).forward(torch.mul(pf_features, pf_mask), )
    _1 = torch.mul(_0, pf_mask)

Traceback of TorchScript, original code (most recent call last):
  File "/app/weaver/utils/nn/model/ParticleNet.py", line 279, in forward
            sv_features *= sv_mask

        points = torch.cat((pf_points, sv_points), dim=2)
                 ~~~~~~~~~ <--- HERE
        features = torch.cat((self.pf_conv(pf_features * pf_mask) * pf_mask, self.sv_conv(sv_features * sv_mask) * sv_mask), dim=2)
        mask = torch.cat((pf_mask, sv_mask), dim=2)
RuntimeError: CUDA error: invalid argument
CUDA kernel errors might be asynchronously reported at some other API call, so the stacktrace below might be incorrect.
For debugging consider passing CUDA_LAUNCH_BLOCKING=1.
Compile with `TORCH_USE_CUDA_DSA` to enable device-side assertions.


%MSG
%MSG-w TritonFailure:  (NoModuleName) 07-Jul-2026 19:09:04 UTC pre-events
An exception of category 'TritonFailure' occurred.
Exception Message:
evaluate(): unable to get result(s): pinned buffer: failed to perform CUDA copy: invalid argument
```